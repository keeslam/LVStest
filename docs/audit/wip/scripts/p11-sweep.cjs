// Phase 11: whole-DB stale-state sweep (repeat of maintenance-transport.md item 7 + transport-specific checks).
'use strict';
const { q, pool } = require('./db.cjs');
(async () => {
  const one = async (label, sql) => { try { const r = await q(sql); console.log(label, JSON.stringify(r)); } catch (e) { console.log(label, 'ERR', e.message); } };
  await one('A dangling replacements (orig deleted/missing)', "select count(*)::int as n, array_agg(r.id order by r.id) as ids from reservations r left join reservations orig on orig.id=r.replacement_for_reservation_id where r.type='replacement' and r.deleted_at is null and r.replacement_for_reservation_id is not null and (orig.id is null or orig.deleted_at is not null)");
  await one('A2 replacements pointing at missing transport', "select count(*)::int as n, array_agg(r.id order by r.id) as ids from reservations r left join vehicle_transports t on t.id=r.replacement_for_transport_id where r.type='replacement' and r.deleted_at is null and r.replacement_for_transport_id is not null and t.id is null");
  await one('A3 live replacements with neither rental nor transport link', "select count(*)::int as n, array_agg(id order by id) as ids from reservations where type='replacement' and deleted_at is null and status not in ('cancelled','completed','returned') and replacement_for_reservation_id is null and replacement_for_transport_id is null");
  await one('B stale TBD placeholders past end date', "select count(*)::int as n, array_agg(id order by id) as ids from reservations where type='replacement' and placeholder_spare=true and vehicle_id is null and deleted_at is null and status not in ('cancelled') and end_date::date < current_date");
  await one('B2 cancelled rows still flagged placeholder_spare', "select count(*)::int as n, array_agg(id order by id) as ids from reservations where type='replacement' and placeholder_spare=true and deleted_at is null and status='cancelled'");
  await one('C vehicles in_service/needs_* without active block/transport', "select count(*)::int as n, array_agg(v.id order by v.id) as ids from vehicles v where v.maintenance_status in ('in_service','needs_fixing','needs_service') and not exists (select 1 from reservations r where r.vehicle_id=v.id and r.type='maintenance_block' and r.deleted_at is null and coalesce(r.maintenance_status,'scheduled')<>'out') and not exists (select 1 from vehicle_transports t where t.vehicle_id=v.id and t.status not in ('completed','cancelled'))");
  await one('D out blocks whose vehicle still in service', "select count(*)::int as n from reservations r join vehicles v on v.id=r.vehicle_id where r.type='maintenance_block' and r.maintenance_status='out' and r.deleted_at is null and v.maintenance_status in ('needs_fixing','in_service')");
  await one('E status outside machine', "select status, count(*)::int as n from reservations where deleted_at is null and status not in ('booked','picked_up','completed','cancelled','returned') group by 1 order by 1");
  await one('F open transports whose spare reservation is deleted/cancelled', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t join reservations r on r.id=t.spare_reservation_id where (r.deleted_at is not null or r.status='cancelled') and t.status not in ('completed','cancelled')");
  await one('F2 cancelled transports with a still-booked spare reservation', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t join reservations r on r.id=t.spare_reservation_id where r.deleted_at is null and r.status='booked' and t.status in ('cancelled')");
  await one('F3 transports whose spare reservation start <> scheduled_date', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t join reservations r on r.id=t.spare_reservation_id where r.deleted_at is null and r.status not in ('cancelled') and r.start_date <> t.scheduled_date");
  await one('F4 transports with vehicle_id = related_vehicle_id', "select count(*)::int as n, array_agg(id) as ids from vehicle_transports where vehicle_id is not null and vehicle_id=related_vehicle_id");
  await one('F5 transports with related_vehicle_id but spare_required=false or no spare reservation', "select count(*)::int as n, array_agg(id) as ids from vehicle_transports where related_vehicle_id is not null and (spare_required=false or spare_reservation_id is null)");
  await one('F6 transports with status outside scheduled/in_progress/completed/cancelled', "select status, count(*)::int as n from vehicle_transports where status not in ('scheduled','in_progress','completed','cancelled') group by 1");
  await one('F7 completed transports with completed_date null', "select count(*)::int as n from vehicle_transports where status='completed' and completed_date is null");
  await one('F8 transport -> spare reservation whose back-link differs', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t join reservations r on r.id=t.spare_reservation_id where r.replacement_for_transport_id is distinct from t.id");
  await one('G transports whose vehicle is deleted/missing', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t left join vehicles v on v.id=t.vehicle_id where t.is_external_vehicle=false and v.id is null");
  await one('G2 transports whose related vehicle is deleted/missing', "select count(*)::int as n, array_agg(t.id) as ids from vehicle_transports t left join vehicles v on v.id=t.related_vehicle_id where t.related_vehicle_id is not null and v.id is null");
  await one('H vehicles flagged for a transport that is gone/closed', "select count(*)::int as n, array_agg(v.id) as ids from vehicles v where v.maintenance_note like 'Replacement vehicle required for transport #%' and not exists (select 1 from vehicle_transports t where t.id = (regexp_match(v.maintenance_note, '#([0-9]+)'))[1]::int and t.status not in ('completed','cancelled'))");
  await one('I live rentals decision=spare_assigned but no live replacement', "select count(*)::int as n, array_agg(r.id) as ids from reservations r where r.deleted_at is null and r.spare_assignment_decision='spare_assigned' and r.status in ('booked','picked_up') and not exists (select 1 from reservations s where s.replacement_for_reservation_id=r.id and s.deleted_at is null and s.status not in ('cancelled'))");
  await one('J live spare reservations for a cancelled/completed rental', "select count(*)::int as n, array_agg(s.id) as ids from reservations s join reservations r on r.id=s.replacement_for_reservation_id where s.type='replacement' and s.deleted_at is null and s.status in ('booked','pending','active') and r.status in ('cancelled','completed','returned')");
  await one('K multiple live spares for one rental', "select replacement_for_reservation_id as rental, count(*)::int as n, array_agg(id) as ids from reservations where type='replacement' and deleted_at is null and status in ('booked','pending','active') and replacement_for_reservation_id is not null group by 1 having count(*)>1 order by 1");
  await one('L blocks whose affected rental now has another vehicle', "select count(*)::int as n, array_agg(b.id) as ids from reservations b join reservations r on r.id=b.affected_rental_id where b.type='maintenance_block' and b.deleted_at is null and r.vehicle_id <> b.vehicle_id");
  await one('M overlapping live maintenance blocks on one vehicle (pairs)', "select count(*)::int as n from reservations a join reservations b on a.vehicle_id=b.vehicle_id and a.id<b.id and a.type='maintenance_block' and b.type='maintenance_block' and a.deleted_at is null and b.deleted_at is null and a.start_date<=coalesce(b.end_date,b.start_date) and b.start_date<=coalesce(a.end_date,a.start_date)");
  await one('N vehicle_transports columns', "select string_agg(column_name, ',' order by ordinal_position) from information_schema.columns where table_name='vehicle_transports'");
  await one('O log-ish tables', "select string_agg(table_name, ',' order by table_name) from information_schema.tables where table_schema='public' and (table_name like '%hist%' or table_name like '%audit%' or table_name like '%activity%' or table_name like '%log%' or table_name like '%deleted%' or table_name like '%notif%' or table_name like '%mail%' or table_name like '%queue%')");
  await one('P vehicles with availability scheduled but no live future reservation', "select count(*)::int as n, array_agg(v.id) as ids from vehicles v where v.availability_status='scheduled' and not exists (select 1 from reservations r where r.vehicle_id=v.id and r.deleted_at is null and r.status in ('booked','pending','active') and coalesce(r.end_date, r.start_date)::date >= current_date)");
  await pool.end();
})().catch(e => { console.error(e); process.exit(1); });
