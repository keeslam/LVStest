# Database Connection
DATABASE_URL=postgresql://username:password@localhost:5432/carrentaldb

# Environment
NODE_ENV=production

# Session Secret (generate a strong random string)
SESSION_SECRET=your_session_secret_here

# Port (optional, defaults to 5000)
# PORT=5000