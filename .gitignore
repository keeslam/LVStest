node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local secrets - never commit real credentials
.env

# Exclude uploaded files and assets - these should not be in Git
uploads/
attached_assets/
contracts/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local git worktrees (scratch, not part of the repo)
.worktrees/