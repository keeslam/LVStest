node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Exclude uploaded files and assets - these should not be in Git
uploads/
attached_assets/
contracts/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Environment files with secrets (keep .env.sample / .env.coolify templates)
.env
.env.local
.env.*.local
